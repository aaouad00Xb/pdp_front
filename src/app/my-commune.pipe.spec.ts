import { MyCommunePipe } from './my-commune.pipe';

describe('MyCommunePipe', () => {
  it('create an instance', () => {
    const pipe = new MyCommunePipe();
    expect(pipe).toBeTruthy();
  });
});
